<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>MusicTrackGen.Core</RootNamespace>
    <!-- No NuGet dependencies: every synth, filter, FFT and file format in this
         assembly is implemented in-repo so the PoC restores and builds offline. -->
  </PropertyGroup>

</Project>
